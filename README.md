## Build Dependencies

openSUSE

sudo zypper install gtk4-devel pipewire-devel pkgconf-pkg-config

Fedora

sudo dnf install gtk4-devel pipewire-devel pkgconf-pkg-config

Debian

sudo apt install libgtk-4-dev libpipewire-0.3-dev pkg-config
